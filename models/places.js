// BEEF NOTE:
// A model to add cities and countries together as nested objects
// feature not yet ready

// const mongoose = require('mongoose');

// const citySchema = new mongoose.Schema({
//     name: String,
// });

// const countrySchema = new mongoose.Schema({
//     name: String,
//     cities: [citySchema],
// });


// module.exports = mongoose.model('Destination', countrySchema)
